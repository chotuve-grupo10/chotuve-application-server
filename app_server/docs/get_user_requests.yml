Este servicio permite obtener las solicitudes de amistad de un usuario
---
definitions:
  UsersInformationList:
    type: array
    items:
      type: object
      properties:
        email:
          type: string
        fullName:
          type: string
      example:
        email: 'diegote@gmail.com'
        fullName: 'Diegote'
parameters:
  - in: path
    name: user_email
    description: email del usuario
    type: string
      
responses:
  200:
    description: se envian las solicitudes de amistad pendientes
    schema:
      $ref: '#/definitions/UsersInformationList'
