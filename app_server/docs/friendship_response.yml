Este servicio permite aceptar una solicitud de contacto de usuario y crear una relación de amistad
---
definitions:
  AcceptFriend:
    type: object
    properties:
      response:
        type: boolean
  RespondFriendshipResponse:
    type: object
    properties:
      status:
        type: integer
      message_result:
        type: object
parameters:
  - in: path
    name: user_email
    description: my email
    type: string
  - in: path
    name: new_friends_email
    description: potential new friends email
    type: string
  - in: body
    name: response_body
    description: Document containing true for accept, false for reject.
    schema:
      $ref: '#/definitions/AcceptFriend'
      
responses:
  201:
    description: solicitud completada
    schema:
      $ref: '#/definitions/RespondFriendshipResponse'
  403:
    description: alguno de los ids no existe
    schema:
      $ref: '#/definitions/RespondFriendshipResponse'
  409:
    description: la solicitud de contacto no existe
    schema:
      $ref: '#/definitions/RespondFriendshipResponse'
  500:
    description: solicitud rechazada por error interno del server
    schema:
      $ref: '#/definitions/RespondFriendshipResponse'