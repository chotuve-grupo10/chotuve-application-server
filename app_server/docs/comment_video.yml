Este servicio permitirá dar de alta un comentario en un video
---
definitions:
  CommentVideoResponse:
    type: object
    properties:
      status:
        type: integer
      message_result:
        type: object
parameters:
  - in: path
    name: video_id
    description: id del video
    type: string
    
  - in: body
    name: user
    description: User making comment.
    schema:
      $ref: '#/definitions/Comment'
responses:
  200:
    description: comentario agregado al video
    schema:
      $ref: '#/definitions/CommentVideoResponse'
