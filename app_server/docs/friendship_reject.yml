Este servicio permite rechazar una solicitud de contacto de usuario
---
definitions:
  RejectFriendshipResponse:
    type: object
    properties:
      status:
        type: integer
      message_result:
        type: object
parameters:
  - in: path
    name: user_id
    description: my id
    type: string
  - in: path
    name: new_friends_id
    description: potential new friends id
    type: string
      
responses:
  201:
    description: solicitud completada
    schema:
      $ref: '#/definitions/RejectFriendshipResponse'
  403:
    description: alguno de los ids no existe
    schema:
      $ref: '#/definitions/RejectFriendshipResponse'
  409:
    description: la solicitud de contacto no existe
    schema:
      $ref: '#/definitions/RejectFriendshipResponse'
  500:
    description: solicitud rechazada por error interno del server
    schema:
      $ref: '#/definitions/RejectFriendshipResponse'