Este servicio permite obtener información del usuario (y sus amigos)
---
definitions:
  FriendsInformation:
    type: object
    properties:
      email:
        type: string
      fullName:
        type: string
parameters:
  - in: path
    name: user_email
    description: email del usuario
    schema:
       type: string
      
responses:
  200:
    description: se envia la información
    type: array
    items:
      schema:
        $ref: '#/definitions/FriendsInformation'